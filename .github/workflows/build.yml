name: apk

on:
  workflow_dispatch:

jobs:
  apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show workflow files
        run: find .github/workflows -type f

      - name: Fake test
        run: echo "FUNCIONOU NOVO WORKFLOW"